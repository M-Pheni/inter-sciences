{{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" value="{{ submitButtonText ?? 'Créer l\'article' }}" class="btn btn-primary btn-block" formnovalidate>
{{ form_end(form) }}