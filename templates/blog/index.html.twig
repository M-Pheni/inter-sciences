{% extends 'base.html.twig' %}

{% block title 'Inter-Sciences : Blog'%}

{% block body %}

<div class="example-wrapper">
    <h1>Blog</h1>
    <h2> {{ pluralize(post|length, 'Article')}}</h2>

    {% for post in post %}
        <article>
            <h1> {{ post.title }} </h1>
            <p> {{ post.content }} </p>
        </article>
    {% endfor %}
</div>
{% endblock %}
